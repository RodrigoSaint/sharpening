import Card from "@rodrigosaint/card";

interface {{componentProps}} {
    {{instance}}: {{type}}
}

export default function {{component}}({ {{instance}} }: {{componentProps}}) {
    return <Card>
        {{fieldCollection}}
    </Card>
}
