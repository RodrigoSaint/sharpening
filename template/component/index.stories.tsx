
import React from "react";
import { Meta } from "@storybook/react";

import {{capital name}} from "../packages/{{ underscore name }}/source";

export default {
  title: "{{ capital name }}",
  component: {{ capital name }},
} as Meta;

export const Default{{capital name}} = () => (
  <{{capital name}} />
);

